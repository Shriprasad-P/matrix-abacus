/// Shared enums for Matrix Abacus domain models.
enum WorksheetStatus { isNew, inProgress, completed }

enum LevelState { locked, unlocked, current, completed }

enum AttendanceStatus { present, absent, holiday, none }

enum PaymentStatus { paid, due, overdue }

enum AnnouncementPriority { normal, important }

enum UiDemoState { normal, loading, empty, error }

enum PracticeDifficulty { easy, medium, challenge }

enum AnswerFeedback { none, correct, incorrect }
